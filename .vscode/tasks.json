{
    "tasks": [
        {
            "type": "shell",
            "label": "make",
            "command": "make -B",
            "options": {
                "cwd": "${workspaceFolder}"
            }
        }
    ],
    "version": "2.0.0"
}